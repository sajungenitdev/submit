import Register from '@/components/shared/Register/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register />
        </div>
    );
};

export default page;