import SettingsPage from '@/components/Dashboard/Settings/SettingsPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <SettingsPage />
        </div>
    );
};

export default page;